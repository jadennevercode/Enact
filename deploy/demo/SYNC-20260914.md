# Local code sync — 2026-09-14

Source: current tracked and non-ignored local Enact files, including uncommitted changes. Temporary Office lock files excluded. Previous `deploy/instance` backup retained unchanged.

Validation on the source checkout:
- Account bundle tests: 4 passed.
- Focused view tests: 52 passed, 1 failed. The ontology tab test expects `Add ontology`, while the rendered translation is `semantic.agentOntologyAdd`.
- Visual architecture tests: 28 passed, 2 failed (raw text size and unapproved inline styles).
- Typecheck failed in `issue-construction-reports.test.tsx` because construction and run fixtures omit required fields.

These are existing source snapshot failures, not a successful build certification.

The demo account bundle remains the 2026-09-08 snapshot until a separately authorized refreshed export is committed. The previous instance backup has its own timestamp and restore instructions in `deploy/instance`.
