# Post-cleanup snapshot — 2026-09-14

The main database now contains **17 workspaces**, down from 242. Removed 225
verified automation-test workspaces and their associated records. Selection
required a test-generated name, a single matching test-account owner, no
attachment records, and no ontology records. The 19 removed issues were test
or onboarding initialization tasks.

All five demo account workspaces remain: Enact Demo, Ontology Lab,
Quality Traceablity, test, and VOMS. Twelve other workspaces were retained,
including the official catalog, personal workspace, and screenshot/diagnostic
workspaces whose continuing usefulness was not established. User account
records, including test identities, were preserved.

Before cleanup, a local encrypted rollback backup was successfully restored.
The deletion plan was rehearsed on that isolated database. The live cleanup
then ran in one transaction, rechecked the candidate identities and contents,
and verified retained workspace rows, all retained workspace-scoped table rows,
and all user rows were unchanged before committing. Two leftover test upload
directories were separately backed up and removed (four files, 130 bytes).
The pre-cleanup rollback data and its key are local only.

This update replaces the main database archive, backend upload archive,
workspace inventory, attachment inventory and main-table counts. The archive
still uses the existing separately supplied `backup.key`. No key was committed.

Verification of the distributed refreshed snapshot:

- Encrypted main database authenticated and restored to a fresh isolated database.
- 17 restored workspaces; five demo memberships; 134 tables; 53,984 rows.
- 142 issues; 112 attachment records; every attachment found in the 201-file upload archive.
- All published component hashes verified; five archive/account tests passed.

Other components retain their 2026-09-10 capture, including seven additional
historical development/integration databases. Those databases were not cleaned;
their 101 workspace rows are separate histories, not current main workspaces.
Do not restore them into the main database.

The separate `deploy/demo` account bundle still dates to 2026-09-08. The updated
main snapshot here contains current demo data as part of the full instance.
