# Build Evidence — WI-000 <标题>

<!--
编辑权限：build 环节拥有本文件。qa 只读——qa 对本文件的内容采取
"未验证声明"的立场，逐条核实而非采信。
五个章节全部必填，缺一节 qa 的 evidence assurance 就会报缺口。
-->

- 对应 contract 版本：1
- 对应 change-scope 版本：1
- 提交范围：`<起>..<止>`

## 1. 变更摘要

一段话说清做了什么。让复核者在读 diff 之前知道该期待看到什么。

## 2. 需求与标准映射

| 变更 | 文件 | 对应 Criterion | 适用标准 |
|---|---|---|---|
| | | 1.1 | `docs/coding-standards.md#错误处理` |

每个实质变更都要能说出它服务于哪条 criterion。映射不上的变更有两种可能：
它是必要的支撑改动（写明），或者它超出了本次范围（应该拿掉）。

## 3. 未执行事项

Contract 里有、但本次没做的。**这一节为空要写"无"，不要留白**——
留白无法区分"没有未执行事项"和"忘了填"。

| Criterion | 为什么没做 | 后续安排 |
|---|---|---|

## 4. 工具与测试结果

| 检查 | 命令 | 结果 |
|---|---|---|
| lint | | |
| 单元测试 | | |
| 构建 | | |

这些是 Build 自证，**不替代独立 QA**。

## 5. 需要重点复核的风险

主动指出你自己不确定的地方。这一节的价值与诚实度成正比——
掩盖不确定性能让本次 review 更快通过，代价是把问题推到更贵的阶段。

| 风险 | 位置 | 建议怎么验 |
|---|---|---|
