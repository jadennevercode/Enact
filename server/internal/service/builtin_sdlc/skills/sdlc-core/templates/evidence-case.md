# Evidence Case — WI-000 <标题>

<!--
编辑权限：release 环节拥有本文件。
九个章节固定不变——责任人需要每次在同样的位置找到同样的东西。
每条结论指向证据引用，不复述内容；不要粘贴原始 trace。
Release 不应该要求业务负责人读工具调用日志。
-->

- Work Item：WI-000
- contract 版本：1
- 组装时间：

## 1. 业务目标与范围

**要达成什么**：（outcomes 原文）

**包含**：
**明确不包含**：

## 2. Contract 覆盖

| Criterion | 验证方式 | 结果 | 证据 |
|---|---|---|---|
| 1.1 | automated | ✅ | `test-plan.yaml#TC-001`，2026-08-21 |
| 2.1 | manual | ✅ | 王五确认，2026-08-21 |

**必须逐条列全**。有 criterion 没出现在这张表里，确定性预检就该判 FAIL——
缺一条就是缺一块判断依据。

## 3. 接口兼容

- 破坏性变更：有 / 无
- 受影响的消费方：
- 迁移要求：

## 4. 安全与隐私

- 是否触及认证 / 权限 / 个人数据 / 密钥：
- 相关检查结果：

## 5. QA 结果

（通过失败统计、flaky、未覆盖项及理由。指向 `qa-report.md`，不复制全文）

**回归**：本次重跑了哪些既有用例、依据是什么、结果如何。为空要写明谁接受了这个风险。

**按风险选择的说明**：本轮做了哪几类测试、有意不做哪几类及理由（引自 `test-plan.yaml` 的 `risk_based_selection`）。

## 6. 已知例外

| 问题 | 严重度 | 为什么可接受 | owner |
|---|---|---|---|

把已知问题藏在"总体通过"里，是最容易导致事后追责的做法。
这一节为空要写"无"。

## 7. 部署与回退

- **部署方式**：
- **回退方式**：
- **回退后数据状态**：

**回退方案为空时确定性预检直接 FAIL。** 没有回退路径的发布是单向门，
它需要的是一个不同量级的决策，而不是一次常规 Release。

## 8. 监控与支持准备

- 发布后观察什么指标、看多久：
- 异常时谁响应：

## 9. 批准

由 `gates/release.yml` 承载。此处不重复记录，避免两处记录不一致。
