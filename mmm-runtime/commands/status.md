---
description: 项目到哪了 —— 已确认、在做、等你、以及有什么问题
---

用 `orchestrator` 的 `status` 模式，看 `$ARGUMENTS` 指向的项目目录（默认当前目录）。

按顺序报四段：**已确认**的交付物、**在做**的那个做到哪一步、**等你**的是什么（这一段最要紧，
说清楚要你做什么、它卡住了什么），以及**问题**——取材被截断的、背后没有行业先例的、
不该为零却为零的计数、以及证据在确认之后又被改过的。

这里没有工作区时，先跑 `state.py list` 再提议新建——用户通常是有项目的，只是站错了地方。
